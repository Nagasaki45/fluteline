Not ready to commit?
====================

So, you want to check fluteline out but don't really want to redesign your entire system around it? That makes a lot of sense! Thankfully, every node has an input and output queues that you can access directly. Take a look at ``examples/with_queues.py`` bellow.

.. literalinclude:: ../examples/with_queues.py
