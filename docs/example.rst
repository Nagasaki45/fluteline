Example
=======

``example.py`` shows how one producer generates random numbers, a consumer-producer passes only the maximal value that goes through it, and a final consumer print the result. Everything runs by it's own and stopped manually after a few seconds.

.. literalinclude:: ../example.py
